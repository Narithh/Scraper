{
  "name": "ChatAndScrape",
  "image": "mcr.microsoft.com/devcontainers/base:jammy",
  "runArgs": [
    "--privileged",
    "--cap-add=ALL"
  ],
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "saoudrizwan.claude-dev"
      ]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y python3 python3-pip xvfb && pip3 install playwright==1.58.0 readability-lxml==0.8.4.1 beautifulsoup4==4.14.3 bs4==0.0.2 lxml==6.0.2 && playwright install --with-deps"
}